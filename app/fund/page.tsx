import { desc } from 'drizzle-orm'; import { WalletCards } from 'lucide-react'; import { getDb } from '../../db'; import { transactions } from '../../db/schema'; import { AccessPending, PortalPage } from '../components/portal-page'; import { getCurrentMember } from '../lib/authz';
export const dynamic='force-dynamic';
export default async function FundPage(){const member=await getCurrentMember();if(!member.approved)return <AccessPending member={member}/>;const rows=await getDb().select().from(transactions).orderBy(desc(transactions.occurredOn));const income=rows.filter(r=>r.type==='income'&&r.status!=='rejected').reduce((s,r)=>s+r.amountCents,0);const expense=rows.filter(r=>r.type==='expense'&&(r.status==='approved'||r.status==='paid')).reduce((s,r)=>s+r.amountCents,0);return <PortalPage member={member} active="/fund" eyebrow="TRANSPARENT TEAM ACCOUNTS" title="Team fund"><div className="metric-row"><div className="metric"><span>Available balance</span><strong>€{((income-expense)/100).toFixed(2)}</strong></div><div className="metric"><span>Total money in</span><strong>€{(income/100).toFixed(2)}</strong></div><div className="metric"><span>Approved spend</span><strong>€{(expense/100).toFixed(2)}</strong></div></div><article className="panel"><div className="section-heading"><div><span>PUBLIC LEDGER</span><h3>Every approved movement of team money</h3></div></div>{rows.length===0?<div className="empty-state"><WalletCards/><strong>No transactions yet</strong><p>The ledger will update as contributions and expenses are recorded.</p></div>:<table className="data-table"><thead><tr><th>Date</th><th>Description</th><th>Category</th><th>Status</th><th>Amount</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.occurredOn}</td><td>{r.description}{member.role!=='parent'&&<small>{r.personName} · {r.paymentMethod||'method not recorded'}</small>}</td><td>{r.category}</td><td><span className={`badge ${r.status}`}>{r.status}</span></td><td className={r.type==='income'?'money-in':'money-out'}>{r.type==='income'?'+':'−'}€{(r.amountCents/100).toFixed(2)}</td></tr>)}</tbody></table>}</article></PortalPage>}
